{
  "name": "abrir-game-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ABRIR — deterministic Three.js action-looter vertical slice.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node scripts/check-project.mjs",
    "generate:map": "node scripts/generate-map.mjs"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
