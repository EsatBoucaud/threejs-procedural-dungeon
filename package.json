{
  "name": "abrir-game-prototype",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "description": "ABRIR — deterministic Three.js action-looter with multiplayer ownership, player-chosen combat kits, and unrestricted field activity authority.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node scripts/check-project.mjs",
    "check:generation": "node scripts/check-generation.mjs",
    "check:headquarters": "node scripts/check-headquarters.mjs",
    "check:processes": "node scripts/check-processes.mjs",
    "check:layout": "node scripts/check-layout.mjs",
    "check:deployment": "node scripts/check-deployment.mjs",
    "check:activities": "node scripts/check-activity-authority.mjs",
    "generate:map": "node scripts/generate-map.mjs"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
